<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/contact_tag_item_height"
    android:background="@drawable/bg_list_item_divider"
    >
    <ImageView
        android:id="@+id/item_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/icon_item_add"
        android:layout_centerVertical="true"
        />
    <TextView
        android:id="@+id/item_tag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/item_icon"
        android:paddingLeft="@dimen/contact_tag_item_padding"
        android:textColor="@color/bgTitleBar"
        android:layout_centerVertical="true"
        android:textSize="@dimen/font_size_medium"
        android:text="add phone" />
</RelativeLayout>
